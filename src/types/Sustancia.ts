// src/models/Sustancia.ts
export interface Sustancia {
    id: string;
    name: string;
    referencia?: string;
    color?: string;
    peso?: number;
    // Agrega aquí más atributos según necesites
  }