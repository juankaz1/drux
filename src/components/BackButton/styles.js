import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  button: {
    padding: 16,
    backgroundColor: '#ccc',
    borderRadius: 8,
    marginVertical: 8,
  },
  buttonText: {
    color: '#000',
    textAlign: 'center',
    fontSize: 16,
  }
});
